Version: 1
Description: Extract information from the foundation.lock file
------------
// Get information from the lock file
def lockFile = new File(rootProject.projectDir, '../foundation.lock')

def foundation = new groovy.json.JsonSlurper().parseText(lockFile.text);
